"""Streamlit UI layer: render helpers that build HTML fragments.

These functions return HTML strings and have no Streamlit side effects, so they
import cleanly and are unit-testable. The Streamlit wiring itself lives in app.py.
"""
